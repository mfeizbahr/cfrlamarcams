name: Run Code Every Minute

on:
  schedule:
    - cron: '*/1 * * * *' # This cron schedule triggers the workflow every minute

jobs:
  run_code:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    # Add steps to set up your environment, install dependencies, and run your code here
    # Example:
    # - name: Install dependencies
    #   run: npm install
    # 
    # - name: Run code
    #   run: npm start
